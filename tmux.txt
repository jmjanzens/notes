### tmux (terminal multiplexer)
    $ tmux

### commands from within session (GNU screen like tmux.conf overrides usual command key with `^a'

## open vertical split to shell
    ^a, |
## open horizontal split to shell
    ^a, h
## close split (just exit shell)
    ^d

